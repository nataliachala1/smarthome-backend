# Contrato MQTT - Smart Home

Este documento define los topics y payloads que deben usar los dispositivos fisicos para comunicarse con el backend Smart Home.

## 1. Broker MQTT

En entorno local:

```env
MQTT_BROKER_URL=mqtt://localhost:1883